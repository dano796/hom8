# Create low-fidelity wireframes

This is a code bundle for Create low-fidelity wireframes. The original project is available at https://www.figma.com/design/sD5k14o3ae7nYiYyYzrNKP/Create-low-fidelity-wireframes.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
